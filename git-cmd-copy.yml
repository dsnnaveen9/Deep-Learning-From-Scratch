git pull origin main --rebase : To pull the code form repo
git push -u origin main --force : to push the code from local repo to remote repo when confilcts are there

git push -u origin main  : to push the code local repo to remote repo in normal way
// git pull = git fetch + git merge(or) git rebase.
git pull origin main : pull latest code from github, When you want to directly integrate the latest code
git fetch origin main : gets the changes from remote, When you want to review changes before merging
git merge origin/main : applie the changes
git rebase origin/main : Applies your changes on top of the latest from remote







===========================================================================================

Before stargin work 
git chekout origin main
git fetch origin main
git merge origin main
git status
git add .
git commint -m "commit-message"
git push origin main